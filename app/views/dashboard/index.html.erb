<h1>This is your dashboard</h1>
<p>You can add kids and their resources on the forms available.</p>
<p>If you have any question or doubt, please get in contact here.</p>


<div class="container mx-auto flex">
    <div class="w-1/2">
        <ul class="mt-5">
            <% current_user.kids.each do |k| %>
                <li>
                    <%= link_to k.name, "#", class: 'underline text-blue-600' %>
                </li>
            <% end %>
        </ul>
    </div>
    <div class="w-1/2">
        <h3>Your resources list</h3>

        <ul>
            <% current_user.resources.each do |r| %>
                <li>
                    <%= link_to r.name, r.url, target: '_blank', class: 'text-blue-500 underline', title: 'Click to open in new tab' %>
                </li>
            <% end %>
        </ul>
    </div>
</div>

